version: 0.2

phases:
  install:
    commands:
      - npm install -g npm
      - npm install -E
  build:
    commads:
      - npm run build
      - mkdir cdk.out
      - npm run cdk -- synth

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'
